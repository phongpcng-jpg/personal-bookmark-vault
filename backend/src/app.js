import 'dotenv/config'; import Fastify from 'fastify'; import cors from '@fastify/cors'; import cookie from '@fastify/cookie'; import session from '@fastify/session'; import helmet from '@fastify/helmet'; import rateLimit from '@fastify/rate-limit'; import sensible from '@fastify/sensible'; import swagger from '@fastify/swagger'; import swaggerUi from '@fastify/swagger-ui'; import {authRoutes} from './routes/authRoutes.js'; import {bookmarkRoutes} from './routes/bookmarkRoutes.js';
export function buildApp(){const app=Fastify({logger:true});app.register(sensible);app.register(helmet);app.register(cors,{origin:process.env.FRONTEND_ORIGIN||'http://localhost:5173',credentials:true});app.register(cookie);app.register(session,{secret:process.env.SESSION_SECRET||'development-only-change-me-please-32-chars',cookie:{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:8*60*60}});app.register(rateLimit,{max:100,timeWindow:'1 minute'});app.register(swagger,{openapi:{info:{title:'Personal Bookmark Vault V1',version:'1.0.0'}}});app.register(swaggerUi,{routePrefix:'/docs'});app.get('/health',async()=>({status:'ok'}));app.register(authRoutes,{prefix:'/api/auth'});app.register(bookmarkRoutes,{prefix:'/api/bookmarks'});app.setErrorHandler((err,req,reply)=>{req.log.error(err);if(err.validation)return reply.code(400).send({success:false,error:{code:'VALIDATION_ERROR',message:'Invalid request'}});return reply.code(err.statusCode||500).send({success:false,error:{code:err.statusCode?'REQUEST_ERROR':'INTERNAL_ERROR',message:err.statusCode?err.message:'Internal server error'}})});return app;}
